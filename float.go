package types

// TODO

// func SmartParseFloat(str string) (float64, error) {
// 	numDots := strings.Count(str, ".")
// 	numCommas := strings.Count(str, ",")
// 	switch {
// 	case numDots == 0 && numCommas == 0, numDots == 1 && numCommas == 0:
// 		return strconv.ParseFloat(str, 64)

// 	case numDots == 1 && numCommas > 0:
// 		str = strings.Replace(str, ",", "", -1)
// 		return strconv.ParseFloat(str, 64)

// 	case numDots ==

// 	}

// }
